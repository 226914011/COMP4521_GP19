"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const MeldType_1 = require("../../meld/MeldType");
/**
 * Determine if the WinningHand is a CommonHand (Chinese: 平糊).
 * @param inputWinningHand WinningHand to be evaluated.
 * @returns true if the inputWinningHand is a CommonHand (Chinese: 平糊).
 */
function isCommonHand(inputWinningHand) {
    const melds = inputWinningHand.getMelds();
    for (const meld of melds) {
        if (meld.getMeldType() === MeldType_1.MeldType.EYES)
            continue;
        if (meld.getMeldType() !== MeldType_1.MeldType.CHOW) {
            return false;
        }
    }
    return true;
}
exports.default = isCommonHand;

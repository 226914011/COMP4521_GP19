"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const MeldType_1 = require("../../meld/MeldType");
/**
 * Determine if the WinningHand is an AllInTriplets (Chinese: 對對糊).
 * @param inputWinningHand WinningHand to be evaluated.
 * @returns true if the inputWinningHand is an AllInTriplets (Chinese: 對對糊).
 */
function isAllInTriplets(inputWinningHand) {
    const melds = inputWinningHand.getMelds();
    for (const meld of melds) {
        if (meld.getMeldType() === MeldType_1.MeldType.EYES)
            continue;
        const isKong = meld.getMeldType() === MeldType_1.MeldType.KONG;
        const isPong = meld.getMeldType() === MeldType_1.MeldType.PONG;
        const isNeitherKongNorPong = !(isKong || isPong);
        if (isNeitherKongNorPong) {
            return false;
        }
    }
    return true;
}
exports.default = isAllInTriplets;
